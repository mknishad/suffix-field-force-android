<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.suffix.fieldforce">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.home.LoginActivity"></activity>
        <activity
            android:name=".activity.WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TaskDashboardActivity" />
        <activity
            android:name=".activity.BillDetailsActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".activity.AddBillActivity" />
        <activity android:name=".activity.TaskDetailsActivity" />
        <activity android:name=".activity.task.TaskDashboard" />
        <activity android:name=".activity.InventoryActivity" />
        <activity android:name=".activity.TaskListActivity" />
        <activity android:name=".activity.BillsActivity" />
        <activity android:name=".activity.task.PreviewTaskActivity" />
        <activity android:name=".activity.home.MainDashboard" />
        <activity android:name=".activity.MainActivity" />

        <service android:name=".networking.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>